"""Thin FastAPI demo wrapper around the backtesting engine.

Phase 3 lands ``main.py`` exposing ``/strategies``, ``/run``, ``/status/{id}``,
``/history``. Auth is intentionally stripped — this is a demo API, not the
authenticated monorepo API.
"""
