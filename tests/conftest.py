from __future__ import annotations

# Standalone research agent: the legacy broker `MockBroker` fixture was removed with the `broker`
# package. The kept tests (ai/research, ai, auth, backtesting, db) bring their own fixtures.
